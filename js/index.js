/**
 * index.js — Página inicial
 *
 * Nenhuma lógica complexa aqui: a index só exibe
 * os cards de navegação. Este arquivo existe para
 * manter consistência com a estrutura do projeto.
 */

document.addEventListener('DOMContentLoaded', () => {
  // Página inicial não precisa de lógica de dados,
  // os links dos cards já são definidos no HTML.
  console.log('CineSystem carregado.');
});
